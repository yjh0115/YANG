# QUAD LAB 배포 전 점검표

> 개발 브랜치 `quad-lab-refresh`에서 확인한 뒤, 교사 최종 검토를 거쳐 공개 브랜치에 반영합니다. 이 문서는 테스트 계획이며 실행 결과를 의미하지 않습니다.

## 1. 기본 이동과 진도

- [ ] 첫 방문: 학습 목록이 열리고 6개 탐구와 관계 지도·최종 미션 링크가 보인다.
- [ ] 첫 탐구 시작: 탐구 → 발견 → 개념 정리 → 연습 → 도전 문제 → 학습 완료 순서로 이동한다.
- [ ] 이전 단계로 돌아갔다가 다시 진행해도 화면과 단계 표시가 일치한다.
- [ ] 도전 문제에서 답을 고르지 않고 `학습 마치기`를 누르면 완료 처리되지 않고 답 선택 안내가 보인다.
- [ ] 오답 선택 후 힌트 및 `정답과 해설 확인`을 이용할 수 있고 학습을 마칠 수 있다.
- [ ] 정답 선택 시 풀이가 표시되고 학습을 마칠 수 있다.
- [ ] 완료한 탐구에 재입장하면 답을 다시 고르지 않아도 완료 화면으로 이동할 수 있다.
- [ ] 도전 문제에서 답을 고른 뒤 이전 단계로 이동했다가 복귀하면 답 선택 기록이 유지된다.
- [ ] 새로고침 후 진행 단계와 완료한 탐구 수가 유지된다.

## 2. 사각형 관계 지도

- [ ] 답 카드를 선택한 뒤 빈칸에 넣을 수 있고 동일한 카드를 여러 빈칸에 사용할 수 있다.
- [ ] 일부만 맞히면 맞힌 빈칸만 초록색으로 표시되고 학습 완료로 기록되지 않는다.
- [ ] 7개 빈칸을 직접 모두 맞히고 `정답 확인`을 누르면 관계 지도 완료가 기록된다.
- [ ] 완료 후 학습 목록으로 돌아가면 관계 지도 완료 안내가 보인다.
- [ ] 처음 방문한 상태에서 `완성 지도 보기`만 눌러도 학습 완료로 기록되지 않는다.
- [ ] 완성 지도를 본 후 `정답 확인`을 눌러도 완료로 기록되지 않는다.
- [ ] `다시 도전`으로 초기화하고 직접 7개를 맞히면 완료 처리된다.
- [ ] 이미 완료한 학습자가 복습용 완성 지도를 열어도 기존 완료 기록은 유지된다.

## 3. 최종 미션

- [ ] 답을 선택하지 않은 상태에서 채점하면 남은 문제 수가 안내되고 점수와 정답이 공개되지 않는다.
- [ ] 일부 문항만 답한 상태에서 채점하면 첫 미응답 문항으로 초점이 이동한다.
- [ ] 10문제 모두 답한 뒤 채점하면 점수와 각 문항 해설이 표시된다.
- [ ] 채점 후 답을 변경하면 이전 점수·해설·이동 링크가 사라지고 재채점 안내가 보인다.
- [ ] 재채점하면 변경된 답에 따라 점수가 다시 계산된다.
- [ ] `다시 풀기`를 누르면 답과 채점 표시가 초기화된다.

## 4. 접근성과 화면 크기

- [ ] 6번째 탐구 관계도에서 Tab으로 각 도형 이름에 접근하고 Enter·스페이스바로 설명을 연다.
- [ ] 관계도에서 다른 도형을 선택하면 선택 상태가 한 곳에만 표시된다.
- [ ] 키보드만으로 답 카드·빈칸·정답 확인·다시 도전을 조작할 수 있다.
- [ ] 모바일 폭에서 관계 지도 빈칸·최종 미션 선택지·이동 버튼이 잘리지 않는다.
- [ ] 화면 읽기 프로그램에서 문제 결과 및 관계 설명이 이해 가능한 순서로 전달된다.

## 5. 배포 전 보존 사항

- [ ] `main`과 공개 GitHub Pages는 최종 승인 전 변경하지 않는다.
- [ ] `relation-map.html`의 정사각형 대각선 교점 직각 표시 SVG 경로 `M80 70L90 80L80 90`는 교사 최종 검토 전 수정하지 않는다.
- [ ] 실제 브라우저 테스트 결과(환경·날짜·통과/실패)를 이 문서에 기록한 후 공개 반영 여부를 결정한다.
