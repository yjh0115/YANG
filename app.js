const seg=x=>`<span class="seg" role="img" aria-label="선분 ${x}">${x}</span>`;
const lessons=[
 {id:1,title:'평행사변형의 성질',goal:'움직여도 변하지 않는 평행사변형의 세 가지 성질을 발견해요.',time:'약 15분',type:'parallelogram',accent:'#6d4aff',concepts:[`<span class="math-run">${seg('AB')} = ${seg('CD')}, ${seg('BC')} = ${seg('DA')}예요.</span>`,`<span class="math-run">∠A = ∠C, ∠B = ∠D예요.</span>`, `두 대각선은 서로를 이등분하여 <span class="math-run">${seg('AO')} = ${seg('OC')}, ${seg('BO')} = ${seg('OD')}예요.</span>`],quiz:{q:'평행사변형 ABCD의 대각선의 교점을 O라 할 때 항상 옳은 것은?',opts:[`${seg('AO')} = ${seg('BO')}`,`${seg('AO')} = ${seg('OC')}`,`${seg('AC')} = ${seg('BD')}`,'∠A = ∠B'],answer:1,why:`두 대각선은 서로를 이등분하므로 O는 ${seg('AC')}와 ${seg('BD')} 각각의 중점이고, <span class="math-run">${seg('AO')} = ${seg('OC')}, ${seg('BO')} = ${seg('OD')}예요.</span>`}},
 {id:2,title:'평행사변형이 되는 조건',goal:'조건만 보고 평행사변형이 되는지 논리적으로 판별해요.',time:'약 18분',type:'conditions',accent:'#0ea5e9',concepts:[`${seg('AB')} ∥ ${seg('CD')}, ${seg('BC')} ∥ ${seg('DA')}`,`${seg('AB')} = ${seg('CD')}, ${seg('BC')} = ${seg('DA')}`,`${seg('AB')} ∥ ${seg('CD')}이고 ${seg('AB')} = ${seg('CD')}`,'∠A = ∠C, ∠B = ∠D',`${seg('AO')} = ${seg('OC')}, ${seg('BO')} = ${seg('OD')}`],quiz:{q:'사각형 ABCD가 반드시 평행사변형이 되는 조건은?',opts:[`${seg('AC')} = ${seg('BD')}`,`${seg('AB')} ∥ ${seg('CD')}`,`${seg('AO')} = ${seg('OC')}, ${seg('BO')} = ${seg('OD')}`,`${seg('AB')} = ${seg('BC')}`],answer:2,why:`두 대각선이 서로를 이등분하여 ${seg('AO')} = ${seg('OC')}, ${seg('BO')} = ${seg('OD')}이면 반드시 평행사변형이에요.`}},
 {id:3,title:'직사각형',goal:'크기를 바꾸어도 유지되는 직사각형의 대각선 성질을 찾아요.',time:'약 12분',type:'rectangle',accent:'#f59e0b',concepts:['∠A = ∠B = ∠C = ∠D = 90°예요.','평행사변형의 모든 성질을 가져요.',`두 대각선의 길이가 같아 ${seg('AC')} = ${seg('BD')}이고, 서로를 이등분해요.`],quiz:{q:`직사각형의 대각선 ${seg('AC')}, ${seg('BD')}에 대한 설명으로 옳은 것은?`,opts:[`${seg('AC')} ⟂ ${seg('BD')}`,`${seg('AC')} = ${seg('BD')}`,`${seg('AO')} ≠ ${seg('OC')}`,'네 변의 길이가 모두 같다'],answer:1,why:`직사각형의 두 대각선은 ${seg('AC')} = ${seg('BD')}이고, ${seg('AO')} = ${seg('OC')}, ${seg('BO')} = ${seg('OD')}가 되도록 서로를 이등분해요.`}},
 {id:4,title:'마름모',goal:'마름모의 대각선이 만드는 특별한 관계를 관찰해요.',time:'약 12분',type:'rhombus',accent:'#16a274',concepts:[`${seg('AB')} = ${seg('BC')} = ${seg('CD')} = ${seg('DA')}예요.`,'평행사변형의 모든 성질을 가져요.',`${seg('AC')} ⟂ ${seg('BD')}이고 두 대각선은 서로를 이등분해요.`],quiz:{q:'마름모 ABCD의 두 대각선이 만나는 각 ∠AOB는 몇 도인가요?',opts:['30°','45°','60°','90°'],answer:3,why:`${seg('AC')} ⟂ ${seg('BD')}이므로 ∠AOB = 90°예요.`}},
 {id:5,title:'정사각형',goal:'직사각형과 마름모의 성질이 어떻게 함께 나타나는지 비교해요.',time:'약 14분',type:'square',accent:'#ec4899',concepts:['정사각형은 직사각형이면서 마름모예요.',`${seg('AC')} = ${seg('BD')}예요.`,`${seg('AC')} ⟂ ${seg('BD')}이고 두 대각선은 서로를 이등분해요.`],quiz:{q:'정사각형의 두 대각선이 동시에 가지는 성질은?',opts:['서로 평행하고 길이가 같다',`길이가 같고 ${seg('AC')} ⟂ ${seg('BD')}이며 서로를 이등분한다`,'서로 수직이지만 이등분하지 않는다','길이만 같다'],answer:1,why:`직사각형의 ${seg('AC')} = ${seg('BD')}와 마름모의 ${seg('AC')} ⟂ ${seg('BD')} 성질이 모두 나타나요.`}},
 {id:6,title:'사각형 관계 탐험',goal:'조건을 더하며 여러 사각형의 포함 관계를 연결해요.',time:'약 16분',type:'relations',accent:'#8b5cf6',concepts:['직사각형과 마름모는 모두 평행사변형이에요.','정사각형은 직사각형이면서 마름모예요.','아래 단계의 도형은 위 단계 도형의 성질을 모두 가져요.'],quiz:{q:'다음 명제 중 옳지 않은 것은?',opts:['정사각형은 직사각형이다','직사각형은 평행사변형이다','모든 평행사변형은 직사각형이다','정사각형은 마름모이다'],answer:2,why:'평행사변형 중 네 각이 직각인 것만 직사각형이에요. 반대 방향의 포함은 성립하지 않아요.'}}
];
const phases=['탐구','발견','개념 정리','연습','도전 문제','학습 완료'];
let state=JSON.parse(localStorage.getItem('quadLabProgress')||'{"completed":[],"steps":{},"mission":null}');
let current=0,step=0,drag=null,show={sides:false,angles:false,diags:true};
const $=s=>document.querySelector(s); const save=()=>localStorage.setItem('quadLabProgress',JSON.stringify(state));
function showView(id){document.querySelectorAll('.view').forEach(v=>v.classList.add('hidden'));$(id).classList.remove('hidden');scrollTo({top:0,behavior:'smooth'})}
function renderHome(){const n=state.completed.length,p=Math.round(n/6*100);$('#progressPercent').textContent=p+'%';$('#progressBar').style.width=p+'%';$('#headerProgressBar').style.width=p+'%';$('#headerProgressText').textContent=`${n} / 6 완료`;$('#progressMessage').textContent=n?`${n}개의 탐구를 완료했어요. ${6-n}개가 남았어요!`:'아직 시작 전이에요. 첫 탐구를 시작해 볼까요?';$('#lessonGrid').innerHTML=lessons.map((l,i)=>`<article class="lesson-card ${state.completed.includes(l.id)?'done':''}"><span class="num">0${l.id}</span>${state.completed.includes(l.id)?'<span class="done-badge">✓ 학습 완료</span>':''}<h3>${l.title}</h3><p>${l.goal}</p><div class="card-meta"><span>◷ ${l.time}</span><span>${i<2?'기초':i<5?'핵심':'종합'}</span></div><button class="card-btn" data-lesson="${i}">${state.completed.includes(l.id)?'다시 탐구하기':'학습 시작 →'}</button></article>`).join('');document.querySelectorAll('[data-lesson]').forEach(b=>b.onclick=()=>openLesson(+b.dataset.lesson));const unlocked=n===6;$('#missionBannerBtn').disabled=!unlocked;$('#missionBannerBtn').textContent=unlocked?'마스터 도전 시작 →':'6개 학습을 먼저 완료하세요'}
function openLesson(i){current=i;step=Math.min(state.steps[lessons[i].id]||0,5);showView('#lessonView');renderLesson()}
function renderLesson(){const l=lessons[current];$('#lessonNumber').textContent='0'+l.id;$('#lessonTitle').textContent=l.title;$('#lessonGoal').textContent=l.goal;$('#lessonTime').textContent=l.time;$('#stepDots').innerHTML=phases.map((_,i)=>`<i class="${i===step?'on':i<step?'done':''}"></i>`).join('');$('#phaseTag').textContent=`STEP ${step+1} · ${phases[step]}`;const titles=['도형을 움직여 보세요','어떤 규칙을 발견했나요?','발견한 개념을 정리해요','개념을 확인해 볼까요?','스스로 설명해 보세요','오늘의 탐구 완료!'];$('#phaseTitle').textContent=titles[step];$('#phaseDesc').textContent=step===0?'먼저 설명을 읽지 말고, 직접 움직이며 변하지 않는 규칙을 찾아보세요.':step===1?'관찰한 측정값을 떠올리며 가장 알맞은 추측을 골라 보세요.':step===2?'탐구에서 발견한 규칙을 수학의 언어로 연결해 보세요.':step===3?'틀려도 괜찮아요. 힌트를 단계별로 확인할 수 있어요.':step===4?'배운 성질을 새로운 상황에 적용해 보세요.':'관찰에서 설명까지, 한 번의 수학 탐구를 완성했어요.';$('#prevStep').style.visibility=step?'visible':'hidden';$('#nextStep').innerHTML=step===5?'학습 목록으로 <span>→</span>':step===4?'채점하고 마치기 <span>→</span>':'다음 단계 <span>→</span>';renderArea()}
function renderArea(){const l=lessons[current],a=$('#interactiveArea');if(step===0){if(l.type==='conditions')renderConditions(a);else if(l.type==='relations')renderRelations(a);else renderGeometry(a,l.type);return}if(step===1){a.innerHTML=`<div class="prompt-card"><h3>어떤 규칙을 발견했나요?</h3><p>도형을 움직여 본 결과와 가장 잘 맞는 문장을 선택하세요.</p><div class="answer-grid">${l.concepts.slice(0,2).map((x,i)=>`<button class="choice" data-discover="${i}">${x}</button>`).join('')}<button class="choice" data-discover="x">움직일 때마다 모든 성질이 달라져요.</button></div><div id="discoverFeedback"></div></div>`;document.querySelectorAll('[data-discover]').forEach(b=>b.onclick=()=>{$('#discoverFeedback').innerHTML=`<div class="feedback ${b.dataset.discover==='x'?'bad':'good'}">${b.dataset.discover==='x'?'한 번 더 움직여 일정하게 유지되는 측정값을 찾아보세요.':'좋은 관찰이에요! 도형의 크기나 모양이 달라져도 유지되는 것이 바로 성질이에요.'}</div>`});return}if(step===2){a.innerHTML=`<div class="concept-card"><div class="concept-list">${l.concepts.map((x,i)=>`<div class="concept-item"><span>${i+1}</span><div><b>핵심 성질 ${i+1}</b><br>${x}</div></div>`).join('')}</div></div>`;return}if(step===3||step===4){renderQuiz(a,l,step===4);return}a.innerHTML=`<div class="concept-card" style="text-align:center"><div style="font-size:48px">✓</div><h2>${l.title} 탐구 완료</h2><p>직접 움직이고 추측한 뒤, 개념으로 정리했어요.<br>언제든 다시 돌아와 도형을 움직여 볼 수 있어요.</p></div>`}
function ptsFor(type,t=0){if(type==='rectangle')return [{x:150,y:110},{x:520,y:110},{x:520,y:340},{x:150,y:340}];if(type==='square')return [{x:190,y:80},{x:480,y:80},{x:480,y:370},{x:190,y:370}];if(type==='rhombus')return [{x:335,y:65},{x:540,y:225},{x:335,y:385},{x:130,y:225}];return [{x:150,y:320},{x:250,y:90},{x:540,y:90},{x:440,y:320}]}
function renderGeometry(a,type){a.innerHTML=`<div class="lab-card"><div class="lab-tools"><button class="tool-btn ${show.sides?'on':''}" data-tool="sides">변의 길이 보기</button><button class="tool-btn ${show.angles?'on':''}" data-tool="angles">각의 크기 보기</button><button class="tool-btn ${show.diags?'on':''}" data-tool="diags">대각선 보기</button><span style="margin-left:auto;color:#8a91a2;font-size:12px;padding:8px">● 꼭짓점을 드래그하세요</span></div><svg class="geometry" id="geometry" viewBox="0 0 680 450" role="img" aria-label="드래그할 수 있는 ${lessons[current].title}"></svg><div class="readout" id="readout"></div></div>`;let p=ptsFor(type);drawShape(p,type);document.querySelectorAll('[data-tool]').forEach(b=>b.onclick=()=>{show[b.dataset.tool]=!show[b.dataset.tool];renderGeometry(a,type)});const svg=$('#geometry');svg.onpointerdown=e=>{if(e.target.dataset.i!==undefined){drag=+e.target.dataset.i;svg.setPointerCapture(e.pointerId)}};svg.onpointermove=e=>{if(drag===null)return;const r=svg.getBoundingClientRect(),x=(e.clientX-r.left)*680/r.width,y=(e.clientY-r.top)*450/r.height;if(type==='parallelogram'){if(drag===0){const dx=x-p[0].x,dy=y-p[0].y;p[0]={x,y};p[3].x+=dx;p[3].y+=dy}else if(drag===1){const dx=x-p[1].x,dy=y-p[1].y;p[1]={x,y};p[2].x+=dx;p[2].y+=dy}else{const opp=(drag+2)%4,adj1=(drag+1)%4,adj2=(drag+3)%4;p[drag]={x,y};p[opp]={x:p[adj1].x+p[adj2].x-x,y:p[adj1].y+p[adj2].y-y}}}else if(type==='rectangle'){const left=Math.max(80,Math.min(x,300)),top=Math.max(50,Math.min(y,200));p=[{x:left,y:top},{x:680-left,y:top},{x:680-left,y:450-top},{x:left,y:450-top}]}else if(type==='square'){const s=Math.max(130,Math.min(320,Math.abs(x-335)*2));p=[{x:335-s/2,y:225-s/2},{x:335+s/2,y:225-s/2},{x:335+s/2,y:225+s/2},{x:335-s/2,y:225+s/2}]}else{const dx=Math.max(100,Math.min(270,Math.abs(x-335))),dy=Math.max(80,Math.min(180,Math.abs(y-225)));p=[{x:335,y:225-dy},{x:335+dx,y:225},{x:335,y:225+dy},{x:335-dx,y:225}]}drawShape(p,type)};svg.onpointerup=()=>drag=null;svg.onpointercancel=()=>drag=null}
const dist=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y);function angle(a,b,c){const u={x:a.x-b.x,y:a.y-b.y},v={x:c.x-b.x,y:c.y-b.y};return Math.acos(Math.max(-1,Math.min(1,(u.x*v.x+u.y*v.y)/Math.hypot(u.x,u.y)/Math.hypot(v.x,v.y))))*180/Math.PI}
function drawShape(p,type){const svg=$('#geometry'),labels=['A','B','C','D'],mid={x:(p[0].x+p[2].x)/2,y:(p[0].y+p[2].y)/2};let s=`<polygon points="${p.map(q=>q.x+','+q.y).join(' ')}" fill="#ede9fe" fill-opacity=".7" stroke="#6d4aff" stroke-width="4"/>`;if(show.diags)s+=`<path d="M${p[0].x},${p[0].y}L${p[2].x},${p[2].y}M${p[1].x},${p[1].y}L${p[3].x},${p[3].y}" stroke="#f59e0b" stroke-width="2.5" stroke-dasharray="7 6"/><circle cx="${mid.x}" cy="${mid.y}" r="5" fill="#f59e0b"/><text x="${mid.x+10}" y="${mid.y-8}" font-weight="800">O</text>`;if(type==='rhombus'||type==='square')s+=`<path d="M${mid.x},${mid.y-14}h14v14" fill="none" stroke="#16a274" stroke-width="2"/>`;p.forEach((q,i)=>{if(show.sides){const n=p[(i+1)%4],m={x:(q.x+n.x)/2,y:(q.y+n.y)/2};s+=`<text x="${m.x}" y="${m.y-9}" text-anchor="middle" fill="#4f46e5" font-size="13" font-weight="800">${(dist(q,n)/10).toFixed(1)}</text>`}if(show.angles)s+=`<text x="${q.x+(q.x<335?18:-40)}" y="${q.y+(q.y<225?28:-16)}" fill="#0f8b68" font-size="12" font-weight="800">${Math.round(angle(p[(i+3)%4],q,p[(i+1)%4]))}°</text>`;s+=`<circle class="drag-point" tabindex="0" data-i="${i}" cx="${q.x}" cy="${q.y}" r="10" fill="#fff" stroke="#6d4aff" stroke-width="4"/><text x="${q.x+(q.x<335?-25:14)}" y="${q.y+(q.y<225?-14:25)}" font-size="16" font-weight="900" fill="#302b54">${labels[i]}</text>`});svg.innerHTML=s;const sides=p.map((q,i)=>dist(q,p[(i+1)%4])/10),angs=p.map((q,i)=>angle(p[(i+3)%4],q,p[(i+1)%4]));let values=type==='rectangle'||type==='square'?[[seg('AC'),(dist(p[0],p[2])/10).toFixed(1)],[seg('BD'),(dist(p[1],p[3])/10).toFixed(1)],['∠A',Math.round(angs[0])+'°'],['∠B',Math.round(angs[1])+'°']]:type==='rhombus'?[[seg('AO'),(dist(p[0],mid)/10).toFixed(1)],[seg('OC'),(dist(p[2],mid)/10).toFixed(1)],['∠AOB','90°'],[seg('AB'),sides[0].toFixed(1)]]:[[seg('AB'),sides[0].toFixed(1)],[seg('CD'),sides[2].toFixed(1)],['∠A',Math.round(angs[0])+'°'],['∠C',Math.round(angs[2])+'°']];$('#readout').innerHTML=values.map(v=>`<div class="measure"><small>${v[0]}</small>${v[1]}</div>`).join('')}
function renderConditions(a){const cards=[`${seg('AB')} ∥ ${seg('CD')}, ${seg('BC')} ∥ ${seg('DA')}`,`${seg('AC')} = ${seg('BD')}`,`${seg('AO')} = ${seg('OC')}, ${seg('BO')} = ${seg('OD')}`,`${seg('AB')} ∥ ${seg('CD')}이고 ${seg('AB')} = ${seg('CD')}`];a.innerHTML=`<div class="prompt-card"><h3>평행사변형 판별 연구소</h3><p>반드시 평행사변형이 되는 조건을 아래 연구 노트로 끌어 놓으세요.</p><div class="classification">${cards.map((x,i)=>`<div class="condition" draggable="true" data-c="${i}">${x}</div>`).join('')}</div><div class="drop-zone" id="dropZone">＋ 평행사변형이 되는 조건</div><div id="conditionFeedback"></div></div>`;document.querySelectorAll('.condition').forEach(c=>c.ondragstart=e=>e.dataTransfer.setData('text',c.dataset.c));const z=$('#dropZone');z.ondragover=e=>e.preventDefault();z.ondrop=e=>{e.preventDefault();const i=+e.dataTransfer.getData('text'),ok=i!==1;$('#conditionFeedback').innerHTML=`<div class="feedback ${ok?'good':'bad'}">${ok?'맞아요. 이 조건은 평행사변형을 보장해요.':`${seg('AC')} = ${seg('BD')}인 등변사다리꼴도 있어요. 반드시 평행사변형은 아니에요.`}</div>`;if(ok)z.append(document.querySelector(`[data-c="${i}"]`))}}
function renderRelations(a){a.innerHTML=`<div class="relation-card"><h3>조건을 더해 도형을 바꿔 보세요</h3><p id="relationText">노드를 선택하면 포함 관계와 추가 조건을 확인할 수 있어요.</p><svg class="relation-svg" viewBox="0 0 760 350"><g stroke="#c9c0ff" stroke-width="3" fill="none"><path d="M380 55V115M380 165V215M380 265V315M380 190L210 240M380 190L550 240M210 290L380 330M550 290L380 330"/></g>${[['사각형',380,35],['사다리꼴',380,140],['평행사변형',380,240],['직사각형',210,270],['마름모',550,270],['정사각형',380,330]].map((n,i)=>`<g class="rel-node" data-rel="${i}"><rect x="${n[1]-72}" y="${n[2]-24}" width="144" height="48" rx="12"/><text x="${n[1]}" y="${n[2]+6}" text-anchor="middle">${n[0]}</text></g>`).join('')}</svg></div>`;const txt=['모든 도형의 출발점이에요.',`${seg('AB')} ∥ ${seg('CD')}처럼 한 쌍 이상의 대변이 평행한 사각형이에요.`,`${seg('AB')} ∥ ${seg('CD')}, ${seg('BC')} ∥ ${seg('DA')}예요.`,'평행사변형 + ∠A = 90° → 직사각형',`평행사변형 + ${seg('AB')} = ${seg('BC')} → 마름모`,`직사각형 + ${seg('AB')} = ${seg('BC')} → 정사각형`];document.querySelectorAll('.rel-node').forEach(n=>n.onclick=()=>{document.querySelectorAll('.rel-node').forEach(x=>x.classList.remove('active'));n.classList.add('active');$('#relationText').innerHTML=txt[n.dataset.rel]})}
function renderQuiz(a,l,challenge){a.innerHTML=`<div class="practice-card"><span class="eyebrow dark">${challenge?'CHALLENGE':'PRACTICE'}</span><h3>${l.quiz.q}</h3><div class="quiz-options">${l.quiz.opts.map((x,i)=>`<button data-answer="${i}">${i+1}. ${x}</button>`).join('')}</div><div id="quizFeedback"></div><div class="hint-row"><button class="hint-btn" data-hint="1">힌트 1</button><button class="hint-btn" data-hint="2">힌트 2</button></div></div>`;document.querySelectorAll('[data-answer]').forEach(b=>b.onclick=()=>{$('#quizFeedback').innerHTML=+b.dataset.answer===l.quiz.answer?`<div class="feedback good">정답이에요. ${l.quiz.why}</div>`:`<div class="feedback bad">아직 답을 공개하지 않을게요. 힌트를 확인하고 한 번 더 생각해 보세요.</div>`});document.querySelectorAll('[data-hint]').forEach(b=>b.onclick=()=>{$('#quizFeedback').innerHTML=`<div class="feedback bad">${b.dataset.hint==='1'?'도형을 움직일 때 끝까지 변하지 않았던 값을 떠올려 보세요.':'개념 정리의 마지막 성질과 대각선의 교점에 주목해 보세요.'}</div>`})}
$('#nextStep').onclick=()=>{const l=lessons[current];if(step<5){step++;state.steps[l.id]=Math.max(state.steps[l.id]||0,step);if(step===5&&!state.completed.includes(l.id))state.completed.push(l.id);save();renderLesson()}else{renderHome();showView('#homeView')}};$('#prevStep').onclick=()=>{if(step){step--;renderLesson()}};
$('#backBtn').onclick=()=>{renderHome();showView('#homeView')};$('#homeBtn').onclick=$('#missionBack').onclick=()=>{renderHome();showView('#homeView')};$('#navLearn').onclick=()=>{renderHome();showView('#homeView');setTimeout(()=>$('#lessonGrid').scrollIntoView({behavior:'smooth'}),50)};$('#navMap').onclick=()=>openLesson(5);$('#navMission').onclick=()=>state.completed.length===6?startMission():toast('6개 학습을 먼저 완료해 주세요.');$('#continueBtn').onclick=()=>openLesson(lessons.findIndex(l=>!state.completed.includes(l.id))<0?0:lessons.findIndex(l=>!state.completed.includes(l.id)));$('#missionBannerBtn').onclick=startMission;
const missionQs=lessons.flatMap((l,i)=>[l.quiz,{q:`“${l.concepts[0]}”를 핵심으로 탐구한 차시는?`,opts:[l.title,...lessons.filter(x=>x.id!==l.id).slice(0,3).map(x=>x.title)],answer:0,why:l.concepts[0]}]).slice(0,10);let mq=0,ms=0,domains=Array(6).fill(0);
function startMission(){mq=0;ms=0;domains.fill(0);showView('#missionView');renderMission()}
function renderMission(){if(mq>=10)return renderResult();const q=missionQs[mq];$('#missionContent').innerHTML=`<div class="mission-head"><span class="eyebrow dark">MASTER CHALLENGE</span><h1>사각형 마스터 도전</h1><p>${mq+1} / 10</p></div><div class="mission-question"><h2>${q.q}</h2><div class="quiz-options">${q.opts.map((x,i)=>`<button data-manswer="${i}">${x}</button>`).join('')}</div></div>`;document.querySelectorAll('[data-manswer]').forEach(b=>b.onclick=()=>{if(+b.dataset.manswer===q.answer){ms++;domains[mq%6]++}mq++;renderMission()})}
function renderResult(){state.mission={score:ms,date:new Date().toISOString()};save();$('#missionContent').innerHTML=`<div class="mission-head"><span class="eyebrow dark">RESULT</span><h1>도전 완료!</h1><div class="score-ring" style="--score:${ms*10}%" data-score="${ms*10}점"></div><p>${ms>=8?'훌륭해요! 사각형의 관계를 정확히 이해하고 있어요.':'틀린 영역으로 돌아가 도형을 다시 움직여 보세요.'}</p></div><div class="concept-card"><h3>영역별 이해도</h3>${lessons.map((l,i)=>{const v=Math.min(100,domains[i]*50);return `<div class="domain-bar"><span>${l.title}</span><i style="--w:${v}%"></i><b>${v}%</b></div>`}).join('')}<button class="primary" style="width:100%;margin-top:20px" id="retryMission">다시 도전하기</button></div>`;$('#retryMission').onclick=startMission}
function toast(x){const t=$('#toast');t.textContent=x;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}renderHome();
